import styled from 'styled-components';
import GoogleIcon from '../icons/googleIcon';


const StyledGoogleIcon = styled(GoogleIcon)`
    position: absolute;
    left: 16px;
`;

export default StyledGoogleIcon;
